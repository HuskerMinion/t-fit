# data/

Drop a CSV here and load it with:

```
t-fit --import data/your-file.csv
```

`.gitignore` keeps CSVs in this folder out of git, so your weight history
doesn't end up on GitHub by accident. The folder itself is tracked so the
path exists in a fresh clone.
